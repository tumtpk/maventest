version: 2

jobs:
  build-job:
    docker:
      - image: circleci/openjdk:8-jdk
    working_directory: ~/repo
    steps:
      - checkout
      - run: mvn integration-test

workflows:
  version: 2
  build-deploy:
    jobs:
      - build-job